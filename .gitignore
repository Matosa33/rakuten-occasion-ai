# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.so

# === Virtual envs ===
.venv/
venv/
env/

# === Test / coverage ===
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.cache/

# === Linters ===
.ruff_cache/
.mypy_cache/

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === Secrets ===
.env
.env.local
*.pem
*.key
# Audit 2026-06-05 : pattern générique (couvre infra/k8s/ et tout futur chemin).
**/secrets.yaml

# === Notebooks ===
.ipynb_checkpoints/

# === Docker ===
docker-compose.override.yml

# === Data lourde (DVC géré plus tard, cf. ADR D-006/D-007/D-008) ===
data/raw/*.parquet
data/raw/*.csv
data/raw/*.jsonl
data/raw/*.json
data/raw/cache/
data/raw/full/
# Photos vendeur uploadées (Cycle 17.1, D-035) — jamais committées (R2/vie privée).
data/uploads/
# Dataset photos d'éval (Phase 2) — vraies photos = privé, jamais committées.
data/photos_eval/*
!data/photos_eval/.gitkeep
data/processed/*.parquet
data/processed/**/*.parquet
data/processed/*.json
data/processed/**/*.json
data/embeddings/*.npy
data/embeddings/**/*.npy
data/embeddings/*.bin
data/embeddings/**/*.bin
data/embeddings/*.parquet
data/index/*.index
data/index/**/*.index
data/index/*.faiss
data/fine_tuning/*.jsonl
data/images/
data/models/*.joblib
data/models/*.pkl
data/models/*.pt
data/models/*.bin
models/

# === HuggingFace cache (téléchargements lourds, hors data/raw/cache) ===
~/.cache/huggingface/

# === Audit reports — on garde les .md commit (synthèse humaine), mais pas
# les artefacts lourds régénérables. *_inventory.json reste commité pour
# figer la trace des décisions (méta-first, choix périmètre, etc.).
reports/**/figures/*.png
reports/**/figures/*.svg
reports/figures/*.png
reports/figures/*.svg
!reports/**/.gitkeep
!reports/figures/.gitkeep
reports/**/*_metrics.json
reports/audit_*_metrics.json

# === MLflow / DVC ===
mlruns/
mlartifacts/
mlruns.db
.dvc/cache
.dvc/tmp
dvc_storage/

# === Frontend (P11 — React + Vite à venir) ===
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/.env.local
frontend/.env.*.local

# === Infra (P14 — Docker / Compose / k8s à venir) ===
infra/**/.env
infra/**/secrets/
infra/minio/data/
infra/postgres/data/

# === Airflow (Cycle 12 — orchestration) ===
infra/airflow/logs/
infra/airflow/plugins/*
!infra/airflow/plugins/.gitkeep

# === K8s (Cycle 13.5) ===
# Binaire kind téléchargé localement (cf. infra/k8s/README), pas un dep du repo.
.local/
# Secrets K8s effectifs (gabarit committé sous infra/k8s/secrets.example.yaml).
infra/k8s/secrets.yaml

# === Monitoring (P15) ===
monitoring/prometheus/data/
monitoring/grafana/data/
monitoring/evidently/reports/

# === Autres ===
*.tmp
*.bak
catboost_info/

# === Exceptions : on garde les .dvc metadata files en Git ===
!data/raw/*.dvc
!data/processed/*.dvc
!data/embeddings/*.dvc
!models/**/*.dvc

# === SQLite (Cycle 9.5 persistence) ===
*.db
*.sqlite
*.sqlite3

# === Interne agent / exam (non public, gardé en local) — Cycle 19 ===
BRAIN/
docs/soutenance/
docs/formation/
tests/test_brain_structure.py

# === docs internes (exercices/exam, non public) — Cycle 33 ===
docs/decouverte/
docs/architecture.md
docs/modeles.md
docs/README.md
reports/10_audit_app/

# prep soutenance perso (local, non public)
documentation/SOUTENANCE_PLAN.md
