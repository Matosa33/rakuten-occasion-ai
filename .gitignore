# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.so

# === Virtual envs ===
.venv/
venv/
env/

# === Test / coverage ===
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.cache/

# === Linters ===
.ruff_cache/
.mypy_cache/

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === Secrets ===
.env
.env.local
*.pem
*.key
# Pattern générique (couvre infra/k8s/ et tout futur chemin).
**/secrets.yaml

# === Notebooks ===
.ipynb_checkpoints/

# === Docker ===
docker-compose.override.yml

# === Data lourde (gérée via DVC) ===
data/raw/*.parquet
data/raw/*.csv
data/raw/*.jsonl
data/raw/*.json
data/raw/cache/
data/raw/full/
# Photos vendeur uploadées — jamais committées (vie privée).
data/uploads/
# Dataset photos d'éval — vraies photos = privé, jamais committées.
data/photos_eval/*
!data/photos_eval/.gitkeep
data/processed/*.parquet
data/processed/**/*.parquet
data/processed/*.json
data/processed/**/*.json
data/embeddings/*.npy
data/embeddings/**/*.npy
data/embeddings/*.bin
data/embeddings/**/*.bin
data/embeddings/*.parquet
data/index/*.index
data/index/**/*.index
data/index/*.faiss
data/fine_tuning/*.jsonl
data/images/
data/models/*.joblib
data/models/*.pkl
data/models/*.pt
data/models/*.bin
models/

# === HuggingFace cache (téléchargements lourds, hors data/raw/cache) ===
~/.cache/huggingface/

# === Audit reports — on garde les .md commit (synthèse humaine), mais pas
# les artefacts lourds régénérables. *_inventory.json reste commité pour
# figer la trace des décisions.
reports/**/figures/*.png
reports/**/figures/*.svg
reports/figures/*.png
reports/figures/*.svg
!reports/**/.gitkeep
!reports/figures/.gitkeep
reports/**/*_metrics.json
reports/audit_*_metrics.json

# === MLflow / DVC ===
mlruns/
mlartifacts/
mlruns.db
.dvc/cache
.dvc/tmp
dvc_storage/

# === Frontend (React + Vite) ===
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/.env.local
frontend/.env.*.local

# === Infra (Docker / Compose / k8s) ===
infra/**/.env
infra/**/secrets/
infra/minio/data/
infra/postgres/data/

# === Airflow (orchestration) ===
infra/airflow/logs/
infra/airflow/plugins/*
!infra/airflow/plugins/.gitkeep

# === K8s ===
# Binaire kind téléchargé localement (cf. infra/k8s/README), pas un dep du repo.
.local/
# Secrets K8s effectifs (gabarit committé sous infra/k8s/secrets.example.yaml).
infra/k8s/secrets.yaml

# === Monitoring ===
monitoring/prometheus/data/
monitoring/grafana/data/
monitoring/evidently/reports/

# === Autres ===
*.tmp
*.bak
catboost_info/

# === Exceptions : on garde les .dvc metadata files en Git ===
!data/raw/*.dvc
!data/processed/*.dvc
!data/embeddings/*.dvc
!models/**/*.dvc

# === SQLite (persistance) ===
*.db
*.sqlite
*.sqlite3

# === Interne (non public, gardé en local) ===
BRAIN/
docs/soutenance/
docs/formation/
tests/test_brain_structure.py

# === docs internes (non public) ===
docs/decouverte/
docs/architecture.md
docs/modeles.md
docs/README.md
reports/10_audit_app/

# prep soutenance perso (local, non public)
documentation/SOUTENANCE_PLAN.*

# Audits internes (non publics)
auditV2/
